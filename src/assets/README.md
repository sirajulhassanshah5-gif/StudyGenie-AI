# Assets Directory

This folder holds static assets used across StudyGenie AI, including:
- Images and illustrations (`.jpg`, `.png`, `.webp`)
- SVG icons and vector graphics (`.svg`)
- Custom fonts or static media resources
